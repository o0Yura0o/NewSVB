# 資料與模型（體積太大，不納入版本控制）
# ⚠ 開頭斜線 / 必要：否則會匹配任何深度的同名目錄
# （例：未加 / 時 nsvb/data/ 整包原始碼會被誤擋）
/data/
/checkpoints/
/runs/

# COMMIT_HASH.txt 是「指向某個 commit 的指標」,應放在 repo 外(Drive share)由 agent
# 在 setup 時下載。若 commit 進 repo,會產生雞生蛋邏輯(指向 commit X 的 file 在 commit X 內)
COMMIT_HASH.txt

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# 環境
.env
.venv/
*.conda

# 編輯器
.vscode/settings.json
.idea/
*.swp
*.swo
.DS_Store

# Jupyter
.ipynb_checkpoints/
*.ipynb

# 暫存
*.tmp
*.log
*.bak

*.pptx
.claude/
NSVB-ZH_ver1/
tmp_reference.md
/outputs/